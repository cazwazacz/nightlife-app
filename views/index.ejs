<!DOCTYPE html>
<html ng-app="main">
  <head>
    <meta charset="utf-8">
    <title>Oot the neet?</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="https://fonts.googleapis.com/css?family=Roboto|Raleway" rel="stylesheet">
    <link href="/public/css/main.css" rel="stylesheet" type="text/css">
  </head>
  <body>

    <div ng-controller="results" class="container-fluid">
    <div class="row" style="float: right">
      <div class="col-sm-2"><a href="<%= loggingHref %>"><button class="btn btn-default" ng-click="<%= loginFunc %>"><%= logButton %></button></a></div>
    </div>
   
        <div class="row">
          <div class="col-sm-4 col-sm-offset-4">
            <h1 class="display-1">Oot the neet?</h1>
            <p class="lead">
              Authenticate yourself with Twitter and see where your mates are going tonight.
            </p>
          </div>
        </div>
        <div class="row">
          <form ng-submit="getResults(input)">
          <div class="search-bar col-sm-6 col-sm-offset-3 input-group">
            <input type="text" ng-model="input" name="search" id="input" class="form-control">
            <span class="input-group-btn">
                 <button class="btn btn-default" id="searchButton" type="submit"><i class="glyphicon glyphicon-search"></i></button>
            </span>

          </div>
          </form>
        </div>

        <div class="row">
          <div class="col-sm-6 col-sm-offset-3">
            <div id="results">
              <div ng-repeat="bar in bars track by $index" class="panel panel-default" ng-cloak>
                <div class="panel-body">
                  <div class="bar-photo">
                    <img class="actual-photo" src="{{ bar.photo }}" alt="A picture of bar.">
                  </div>
                 <div class="h4 bar-name">
                   {{ bar.name }} <br> 
                   <span id="rating">Rating: <span id="rating-num">{{bar.rating}}</span></span>

                 </div>
                   <div class="going-button">
                    <!--  <form action="/going" method="post">  -->
                     <form ng-submit="goingThere(bar.id)"> 
                       <!-- <input type="hidden" name="placeId" ng-model="test" value="thisistestvalue"> -->
                       <%- goingButton %>
                       <!-- <a href="/auth/twitter"><input type="button" class="btn btn-default" value="{{ bar.going }} going"></a>
                       <input class="btn btn-default" type="submit" value="{{ bar.going }} going"> -->
                     </form>
                   </div>
                </div>
              </div>
            </div>
          </div>
        </div>
        <hr>
        <footer>Created by Allan Wazacz.</footer>
    </div>
    <script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/angularjs/1.6.1/angular.min.js"></script>
    <script type="text/javascript" src="https://code.angularjs.org/1.3.15/angular-resource.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/ngStorage/0.3.11/ngStorage.min.js"></script>
    <script src="/controllers/searchController.client.js" type="text/javascript"></script>
  </body>
</html>
